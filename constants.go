package main

const (
	accountsFile        = "accounts.json"
	blockchainFile      = "blockchain.json"
	transactionPoolFile = "transaction_pool.json"
	encryptionKey       = "my_secure_password"
	balancesFile        = "balances.json"
)
